@inherits Nancy.ViewEngines.Razor.NancyRazorViewBase<MtgDb.Info.SetsModel>
@{
  Layout = "_Layout.cshtml";
}
@section Top{
	<h2>
		<i class="fa fa-book"></i>&nbsp;
  		Magic: the gathering Set Binders
  	</h2>
}
<div class="col-md-6 col-setlist">
    <div>
    <h3>Base/Core Sets</h3>
    <ul class="setlist">
    @foreach(var set in Model.GetBlock("Core"))
    {
        string imgUrl = string.Format("https://api.mtgdb.info/content/set_images/symbols/{0}_sym.png",set.Id.ToLower());

        <li><span class="seticon"><img src="@imgUrl" /></span>
        <a href="~/sets/@set.Id">@set.Name</a>
        @if(Model.Planeswalker != null){<strong>@Model.GetSetCount(@set.Id).ToString()</strong>}
        <span class="released">Released  @set.ReleasedAt.ToShortDateString()</span></li>
    }
    </ul>
    </div>

    <div>
    <h3>Compilations/Reprint Sets</h3>
    <ul class="setlist">
    @foreach(var set in Model.GetBlock("Non-Block"))
    {
        string imgUrl = string.Format("https://api.mtgdb.info/content/set_images/symbols/{0}_sym.png",set.Id.ToLower());

        <li><span class="seticon"><img src="@imgUrl" /></span>
        <a href="~/sets/@set.Id">@set.Name</a> 
        @if(Model.Planeswalker != null){<strong>@Model.GetSetCount(@set.Id).ToString()</strong>}
        <span class="released">Released @set.ReleasedAt.ToShortDateString()</span></li>

    }
    </ul>
    </div>
    <div>
    <h4>Duel Decks</h4>
    <ul class="setlist">
    @foreach(var set in Model.GetBlock("Duel Decks"))
    {
        string imgUrl = string.Format("https://api.mtgdb.info/content/set_images/symbols/{0}_sym.png",set.Id.ToLower());

        <li><span class="seticon"><img src="@imgUrl" /></span>
        <a href="~/sets/@set.Id">@set.Name</a> 
        @if(Model.Planeswalker != null){<strong>@Model.GetSetCount(@set.Id).ToString()</strong>}
        <span class="released">Released @set.ReleasedAt.ToShortDateString()</span></li>
    }
    </ul>
    </div>
    <div>
    <h4>From the Vault</h4>
    <ul class="setlist">
    @foreach(var set in Model.GetBlock("From the vault"))
    {
        string imgUrl = string.Format("https://api.mtgdb.info/content/set_images/symbols/{0}_sym.png",set.Id.ToLower());

        <li><span class="seticon"><img src="@imgUrl" /></span>
        <a href="~/sets/@set.Id">@set.Name</a> 
        @if(Model.Planeswalker != null){<strong>@Model.GetSetCount(@set.Id).ToString()</strong>}
        <span class="released">Released @set.ReleasedAt.ToShortDateString()</span></li>
    }
    </ul>
    </div>
    <div>
    <h4>Premium Deck Series</h4>
    <ul class="setlist">
    @foreach(var set in Model.GetBlock("Premium Deck Series"))
    {
        string imgUrl = string.Format("https://api.mtgdb.info/content/set_images/symbols/{0}_sym.png",set.Id.ToLower());

        <li><span class="seticon"><img src="@imgUrl" /></span>
        <a href="~/sets/@set.Id">@set.Name</a> 
        @if(Model.Planeswalker != null){<strong>@Model.GetSetCount(@set.Id).ToString()</strong>}
        <span class="released">Released @set.ReleasedAt.ToShortDateString()</span></li>
    }
    </ul>
    </div>
    <div>
    <h4>Planechase</h4>
    <ul class="setlist">
    @foreach(var set in Model.GetBlock("Planechase"))
    {
        string imgUrl = string.Format("https://api.mtgdb.info/content/set_images/symbols/{0}_sym.png",set.Id.ToLower());

        <li><span class="seticon"><img src="@imgUrl" /></span>
        <a href="~/sets/@set.Id">@set.Name</a> 
        @if(Model.Planeswalker != null){<strong>@Model.GetSetCount(@set.Id).ToString()</strong>}
        <span class="released">Released @set.ReleasedAt.ToShortDateString()</span></li>
    }
    </ul>
    </div>

    <div>
    <h4>Commander</h4>
    <ul class="setlist">
    @foreach(var set in Model.GetBlock("Commander"))
    {
        string imgUrl = string.Format("https://api.mtgdb.info/content/set_images/symbols/{0}_sym.png",set.Id.ToLower());

        <li><span class="seticon"><img src="@imgUrl" /></span>
        <a href="~/sets/@set.Id">@set.Name</a> 
        @if(Model.Planeswalker != null){<strong>@Model.GetSetCount(@set.Id).ToString()</strong>}
        <span class="released">Released @set.ReleasedAt.ToShortDateString()</span></li>
    }
    </ul>
    </div>

    <div>
    <h4>Portal</h4>
    <ul class="setlist">
    @foreach(var set in Model.GetBlock("Portal"))
    {
        string imgUrl = string.Format("https://api.mtgdb.info/content/set_images/symbols/{0}_sym.png",set.Id.ToLower());

        <li><span class="seticon"><img src="@imgUrl" /></span>
        <a href="~/sets/@set.Id">@set.Name</a> 
        @if(Model.Planeswalker != null){<strong>@Model.GetSetCount(@set.Id).ToString()</strong>}
        <span class="released">Released @set.ReleasedAt.ToShortDateString()</span></li>
    }
    </ul>
    </div>

    <div>
    <h4>Starter</h4>
    <ul class="setlist">
    @foreach(var set in Model.GetBlock("Starter"))
    {
       string imgUrl = string.Format("https://api.mtgdb.info/content/set_images/symbols/{0}_sym.png",set.Id.ToLower());

       <li><span class="seticon"><img src="@imgUrl" /></span>
       <a href="~/sets/@set.Id">@set.Name</a> 
       @if(Model.Planeswalker != null){<strong>@Model.GetSetCount(@set.Id).ToString()</strong>}
       <span class="released">Released @set.ReleasedAt.ToShortDateString()</span></li>
    }
    </ul>
    </div>

    <div>
    <h4>Un-sets</h4>
    <ul class="setlist">
    @foreach(var set in Model.GetBlock("Un-sets"))
    {
        string imgUrl = string.Format("https://api.mtgdb.info/content/set_images/symbols/{0}_sym.png",set.Id.ToLower());

        <li><span class="seticon"><img src="@imgUrl" /></span>
        <a href="~/sets/@set.Id">@set.Name</a> 
        @if(Model.Planeswalker != null){<strong>@Model.GetSetCount(@set.Id).ToString()</strong>}
        <span class="released">Released @set.ReleasedAt.ToShortDateString()</span></li>
    }
    </ul>
    </div>
</div>
<div class="col-md-6 col-setlist">
    <h3>Expansions</h3>
    <div>
    <h4>Ice Age Cycle/Block</h4>
    <ul class="setlist">
    @foreach(var set in Model.GetBlock("Ice Age"))
    {
        string imgUrl = string.Format("https://api.mtgdb.info/content/set_images/symbols/{0}_sym.png",set.Id.ToLower());

        <li><span class="seticon"><img src="@imgUrl" /></span>
        <a href="~/sets/@set.Id">@set.Name</a> 
        @if(Model.Planeswalker != null){<strong>@Model.GetSetCount(@set.Id).ToString()</strong>}
        <span class="released">Released @set.ReleasedAt.ToShortDateString()</span></li>
    }
    </ul>
    </div>

    <div>
    <h4>Mirage Cycle/Block</h4>
    <ul class="setlist">
    @foreach(var set in Model.GetBlock("Mirage"))
    {
        string imgUrl = string.Format("https://api.mtgdb.info/content/set_images/symbols/{0}_sym.png",set.Id.ToLower());

        <li><span class="seticon"><img src="@imgUrl" /></span>
        <a href="~/sets/@set.Id">@set.Name</a> 
        @if(Model.Planeswalker != null){<strong>@Model.GetSetCount(@set.Id).ToString()</strong>}
        <span class="released">Released @set.ReleasedAt.ToShortDateString()</span></li>
    }
    </ul>
    </div>

    <div>
    <h4>Rath Cycle or Tempest Block</h4>
    <ul class="setlist">
    @foreach(var set in Model.GetBlock("Rath"))
    {
        string imgUrl = string.Format("https://api.mtgdb.info/content/set_images/symbols/{0}_sym.png",set.Id.ToLower());

        <li><span class="seticon"><img src="@imgUrl" /></span>
        <a href="~/sets/@set.Id">@set.Name</a> 
        @if(Model.Planeswalker != null){<strong>@Model.GetSetCount(@set.Id).ToString()</strong>}
        <span class="released">Released @set.ReleasedAt.ToShortDateString()</span></li>    
    }
    </ul>
    </div>

    <div>
    <h4>Artifacts Cycle or Urza Block</h4>
    <ul class="setlist">
    @foreach(var set in Model.GetBlock("Artifacts"))
    {
        string imgUrl = string.Format("https://api.mtgdb.info/content/set_images/symbols/{0}_sym.png",set.Id.ToLower());

        <li><span class="seticon"><img src="@imgUrl" /></span>
        <a href="~/sets/@set.Id">@set.Name</a> 
        @if(Model.Planeswalker != null){<strong>@Model.GetSetCount(@set.Id).ToString()</strong>}
        <span class="released">Released @set.ReleasedAt.ToShortDateString()</span></li>
    }
    </ul>
    </div>

    <div>
    <h4>Masquerade Cycle or Masques Block</h4>
    <ul class="setlist">
    @foreach(var set in Model.GetBlock("Masquerade"))
    {
        string imgUrl = string.Format("https://api.mtgdb.info/content/set_images/symbols/{0}_sym.png",set.Id.ToLower());

        <li><span class="seticon"><img src="@imgUrl" /></span>
        <a href="~/sets/@set.Id">@set.Name</a> 
        @if(Model.Planeswalker != null){<strong>@Model.GetSetCount(@set.Id).ToString()</strong>}
        <span class="released">Released @set.ReleasedAt.ToShortDateString()</span></li>
    }
    </ul>
    </div>

    <div>
    <h4>Invasion Cycle/Block</h4>
    <ul class="setlist">
    @foreach(var set in Model.GetBlock("Invasion"))
    {
        string imgUrl = string.Format("https://api.mtgdb.info/content/set_images/symbols/{0}_sym.png",set.Id.ToLower());

        <li><span class="seticon"><img src="@imgUrl" /></span>
        <a href="~/sets/@set.Id">@set.Name</a> 
        @if(Model.Planeswalker != null){<strong>@Model.GetSetCount(@set.Id).ToString()</strong>}
        <span class="released">Released @set.ReleasedAt.ToShortDateString()</span></li>
    }
    </ul>
    </div>

    <div>
    <h4>Odyssey Cycle/Block</h4>
    <ul class="setlist">
    @foreach(var set in Model.GetBlock("Odyssey"))
    {
        string imgUrl = string.Format("https://api.mtgdb.info/content/set_images/symbols/{0}_sym.png",set.Id.ToLower());

        <li><span class="seticon"><img src="@imgUrl" /></span>
        <a href="~/sets/@set.Id">@set.Name</a> 
        @if(Model.Planeswalker != null){<strong>@Model.GetSetCount(@set.Id).ToString()</strong>}
        <span class="released">Released @set.ReleasedAt.ToShortDateString()</span></li>
    }
    </ul>
    </div>

    <div>
    <h4>Onslaught Cycle/Block</h4>
    <ul class="setlist">
    @foreach(var set in Model.GetBlock("Onslaught"))
    {
        string imgUrl = string.Format("https://api.mtgdb.info/content/set_images/symbols/{0}_sym.png",set.Id.ToLower());

        <li><span class="seticon"><img src="@imgUrl" /></span>
        <a href="~/sets/@set.Id">@set.Name</a> 
        @if(Model.Planeswalker != null){<strong>@Model.GetSetCount(@set.Id).ToString()</strong>}
        <span class="released">Released @set.ReleasedAt.ToShortDateString()</span></li>
    }
    </ul>
    </div>

    <div>
    <h4>Mirrodin Cycle/Block</h4>
    <ul class="setlist">
    @foreach(var set in Model.GetBlock("Mirrodin"))
    {
        string imgUrl = string.Format("https://api.mtgdb.info/content/set_images/symbols/{0}_sym.png",set.Id.ToLower());

        <li><span class="seticon"><img src="@imgUrl" /></span>
        <a href="~/sets/@set.Id">@set.Name</a> 
        @if(Model.Planeswalker != null){<strong>@Model.GetSetCount(@set.Id).ToString()</strong>}
        <span class="released">Released @set.ReleasedAt.ToShortDateString()</span></li>
    }
    </ul>
    </div>

    <div>
    <h4>Kamigawa Cycle/Block</h4>
    <ul class="setlist">
    @foreach(var set in Model.GetBlock("Kamigawa"))
    {
        string imgUrl = string.Format("https://api.mtgdb.info/content/set_images/symbols/{0}_sym.png",set.Id.ToLower());

        <li><span class="seticon"><img src="@imgUrl" /></span>
        <a href="~/sets/@set.Id">@set.Name</a> 
        @if(Model.Planeswalker != null){<strong>@Model.GetSetCount(@set.Id).ToString()</strong>}
        <span class="released">Released @set.ReleasedAt.ToShortDateString()</span></li>
    }
    </ul>
    </div>

    <div>
    <h4>Ravnica Cycle/Block</h4>
    <ul class="setlist">
    @foreach(var set in Model.GetBlock("Ravnica"))
    {
        string imgUrl = string.Format("https://api.mtgdb.info/content/set_images/symbols/{0}_sym.png",set.Id.ToLower());

        <li><span class="seticon"><img src="@imgUrl" /></span>
        <a href="~/sets/@set.Id">@set.Name</a> 
        @if(Model.Planeswalker != null){<strong>@Model.GetSetCount(@set.Id).ToString()</strong>}
        <span class="released">Released @set.ReleasedAt.ToShortDateString()</span></li>
    }
    </ul>
    </div>

    <div>
    <h4>Time Spiral Cycle/Block</h4>
    <ul class="setlist">
    @foreach(var set in Model.GetBlock("Time Spiral"))
    {
        string imgUrl = string.Format("https://api.mtgdb.info/content/set_images/symbols/{0}_sym.png",set.Id.ToLower());

        <li><span class="seticon"><img src="@imgUrl" /></span>
        <a href="~/sets/@set.Id">@set.Name</a> 
        @if(Model.Planeswalker != null){<strong>@Model.GetSetCount(@set.Id).ToString()</strong>}
        <span class="released">Released @set.ReleasedAt.ToShortDateString()</span></li>
    }
    </ul>
    </div>

    <div>
    <h4>Lorwyn Cycle/Block</h4>
    <ul class="setlist">
    @foreach(var set in Model.GetBlock("Lorwyn"))
    {
        string imgUrl = string.Format("https://api.mtgdb.info/content/set_images/symbols/{0}_sym.png",set.Id.ToLower());

        <li><span class="seticon"><img src="@imgUrl" /></span>
        <a href="~/sets/@set.Id">@set.Name</a> 
        @if(Model.Planeswalker != null){<strong>@Model.GetSetCount(@set.Id).ToString()</strong>}
        <span class="released">Released @set.ReleasedAt.ToShortDateString()</span></li>
    }
    </ul>
    </div>

    <div>
    <h4>Shadowmoor Cycle/Block</h4>
    <ul class="setlist">
    @foreach(var set in Model.GetBlock("Shadowmoor"))
    {
        string imgUrl = string.Format("https://api.mtgdb.info/content/set_images/symbols/{0}_sym.png",set.Id.ToLower());

        <li><span class="seticon"><img src="@imgUrl" /></span>
        <a href="~/sets/@set.Id">@set.Name</a> 
        @if(Model.Planeswalker != null){<strong>@Model.GetSetCount(@set.Id).ToString()</strong>}
        <span class="released">Released @set.ReleasedAt.ToShortDateString()</span></li>
    }
    </ul>
    </div>

    <div>
    <h4>Alara Cycle/Block</h4>
    <ul class="setlist">
    @foreach(var set in Model.GetBlock("Shards of Alara"))
    {
        string imgUrl = string.Format("https://api.mtgdb.info/content/set_images/symbols/{0}_sym.png",set.Id.ToLower());

        <li><span class="seticon"><img src="@imgUrl" /></span>
        <a href="~/sets/@set.Id">@set.Name</a> 
        @if(Model.Planeswalker != null){<strong>@Model.GetSetCount(@set.Id).ToString()</strong>}
        <span class="released">Released @set.ReleasedAt.ToShortDateString()</span></li>
    }
    </ul>
    </div>

    <div>
    <h4>Zendikar Cycle/Block</h4>
    <ul class="setlist">
    @foreach(var set in Model.GetBlock("Zendikar"))
    {
        string imgUrl = string.Format("https://api.mtgdb.info/content/set_images/symbols/{0}_sym.png",set.Id.ToLower());

        <li><span class="seticon"><img src="@imgUrl" /></span>
        <a href="~/sets/@set.Id">@set.Name</a> 
        @if(Model.Planeswalker != null){<strong>@Model.GetSetCount(@set.Id).ToString()</strong>}
        <span class="released">Released @set.ReleasedAt.ToShortDateString()</span></li>
    }
    </ul>
    </div>

    <div>
    <h4>Scars of Mirrodin Cycle/Block</h4>
    <ul class="setlist">
    @foreach(var set in Model.GetBlock("Scars of Mirrodin"))
    {
        string imgUrl = string.Format("https://api.mtgdb.info/content/set_images/symbols/{0}_sym.png",set.Id.ToLower());

        <li><span class="seticon"><img src="@imgUrl" /></span>
        <a href="~/sets/@set.Id">@set.Name</a> 
        @if(Model.Planeswalker != null){<strong>@Model.GetSetCount(@set.Id).ToString()</strong>}
        <span class="released">Released @set.ReleasedAt.ToShortDateString()</span></li>
    }
    </ul>
    </div>

    <div>
    <h4>Innistrad Cycle/Block</h4>
    <ul class="setlist">
    @foreach(var set in Model.GetBlock("Innistrad"))
    {
        string imgUrl = string.Format("https://api.mtgdb.info/content/set_images/symbols/{0}_sym.png",set.Id.ToLower());

        <li><span class="seticon"><img src="@imgUrl" /></span>
        <a href="~/sets/@set.Id">@set.Name</a> 
        @if(Model.Planeswalker != null){<strong>@Model.GetSetCount(@set.Id).ToString()</strong>}
        <span class="released">Released @set.ReleasedAt.ToShortDateString()</span></li>
    }
    </ul>
    </div>

    <div>
    <h4>Return to Ravnica Cycle/Block</h4>
    <ul class="setlist">
    @foreach(var set in Model.GetBlock("Return to Ravnica"))
    {
        string imgUrl = string.Format("https://api.mtgdb.info/content/set_images/symbols/{0}_sym.png",set.Id.ToLower());

        <li><span class="seticon"><img src="@imgUrl" /></span>
        <a href="~/sets/@set.Id">@set.Name</a> 
        @if(Model.Planeswalker != null){<strong>@Model.GetSetCount(@set.Id).ToString()</strong>}
        <span class="released">Released @set.ReleasedAt.ToShortDateString()</span></li>
    }
    </ul>
    </div>

    <div>
    <h4>Theros Cycle/Block</h4>
    <ul class="setlist">
    @foreach(var set in Model.GetBlock("Theros"))
    {
        string imgUrl = string.Format("https://api.mtgdb.info/content/set_images/symbols/{0}_sym.png",set.Id.ToLower());

        <li><span class="seticon"><img src="@imgUrl" /></span>
        <a href="~/sets/@set.Id">@set.Name</a> 
        @if(Model.Planeswalker != null){<strong>@Model.GetSetCount(@set.Id).ToString()</strong>}
        <span class="released">Released @set.ReleasedAt.ToShortDateString()</span></li>
    }
    </ul>
    </div>
</div>





